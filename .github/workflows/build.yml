name: Build Hybrid Module

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compile Native Zygisk Binary with Manual NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26b-linux.zip
          unzip -q android-ndk-r26b-linux.zip
          export ANDROID_NDK_HOME=$PWD/android-ndk-r26b

          cd jni
          $ANDROID_NDK_HOME/ndk-build
          cd ..

      - name: Assemble and Package Flat Hybrid ZIP
        run: |
          rm -f *.zip
          rm -rf build_staging
          mkdir -p build_staging/zygisk
          
          # Salin file modul dari module_files
          if [ -d "module_files" ]; then
            cp -rf module_files/* build_staging/
          fi

          # Ambil hasil kompilasi biner .so
          SO_FILE=$(find libs/ obj/ jni/ -name "*.so" 2>/dev/null | head -n 1)
          
          if [ -n "$SO_FILE" ]; then
            cp -f "$SO_FILE" build_staging/zygisk/arm64-v8a.so
            echo "Binary successfully packaged: $SO_FILE -> build_staging/zygisk/arm64-v8a.so"
          else
            echo "CRITICAL ERROR: Tidak ada berkas .so yang berhasil dibuat oleh NDK!"
            exit 1
          fi

          # Kompresi struktur flat root
          cd build_staging
          zip -r ../Root_Assistant_Stable.zip .
          cd ..

      - name: Upload Release Artifact
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v8.0
          files: Root_Assistant_Stable.zip
          overwrite: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
